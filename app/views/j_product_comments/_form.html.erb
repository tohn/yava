<% if current_user %>
<%= form_for(@j_product_comment) do |f| %>
  <% if @j_product_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@j_product_comment.errors.count, "error") %> prohibited this j_product_comment from being saved:</h2>

      <ul>
      <% @j_product_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.number_field :comment_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% else %>
		<%= no_rights %>
<% end %>
