<% provide(:title, t(:manufacturers_title, count: 2)) %>

<h1><%= t(:manufacturers_title, count: 2) %></h1>

<p><%= link_to t("form.new", genus: t("form.c1"), model: t("manufacturers_title.one")), new_manufacturer_path, { class: "btn" } %></p>
<hr>

<div class="center">
	<%= will_paginate @manufacturers %>
</div>

<table class="table table-striped table-condensed">
  <thead>
    <tr>
<!-- 	  <th><%= t("activerecord.attributes.manufacturer.image") %></th> -->
	  <th><%= t("activerecord.attributes.manufacturer.name") %></th>
    </tr>
  </thead>

  <tbody>
    <% @manufacturers.order("name ASC").each do |manufacturer| %>
			<tr>
<!-- 					<td><%= image_tag manufacturer.image.url(:thumb), class: "img-polaroid" %></td> -->
        <td><%= link_to manufacturer.name, manufacturer %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="center">
	<%= will_paginate @manufacturers %>
</div>

<hr>

<p><%= link_to t("form.new", genus: t("form.c1"), model: t("manufacturers_title.one")), new_manufacturer_path, { class: "btn" } %></p>